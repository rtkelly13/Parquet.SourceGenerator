# Release readiness status (#230)

This report records the release status as of 2026-09-17. The repository has a substantial working
default path, but the release evidence is not complete.

## Completed work

The generated API audit, builder, option ownership, API ledger, generated API baselines, Arrow
RecordBatch paths, shared verification, strict physical schema validation, benchmark headline refresh,
test-framework migrations, and the current dependency queue are merged. MSBuild configuration plumbing
(#224) is merged; the feature-level policy implementation (#290, PR #332) remains in the final review
queue.

## Work in review

| Area | State | Evidence |
| --- | --- | --- |
| Hostile-input safety | In review | #306/#319, #307/#325, and #315/#323 add bounds and metadata validation |
| API compatibility | In review | #244/#328 and #246/#329 |
| Arrow/AOT and test evidence | In review | #267/#326, #259/#322, and #258/#327 |
| Configuration policy | In review | #290/#332; #224 is merged |
| Schema-generation optimization | Decision recorded | #291/#331 defers per-type descriptors because measured ELOC increased |
| Generated/shipped boundary | Decision recorded | #237/#334 keeps backend-specific orchestration in generated code |
| Future API seams | Scope recorded | #219–#222, #225–#227, #229, and #223 are reserved for later work |
| Nested compatibility | Scope recorded | #176/#345 records modern nested struct/list support and classic flat-only support |

## Remaining release evidence

The final release evidence has not been produced: current implementation and legacy benchmark
baselines, version/tag/changelog reconciliation, the complete release workflow battery, final public
API baseline promotion, package validation, and tag/package publication remain outstanding.

The exit gate is to merge the open safety, compatibility, and evidence PRs; review the scope decisions;
run the complete release battery from a clean base; reconcile the release metadata; and publish the
package only after those checks pass.
