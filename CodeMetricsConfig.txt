# Code metrics thresholds for the hand-written code under src/ (docs/21-CODE-METRICS.md, #251).
#
# Read by the CA1502 / CA1505 / CA1506 analyzers (enabled at `warning` for [src/**.cs] in
# .editorconfig; CI's `-warnaserror` promotes them), and validated by scripts/CodeMetrics.cs on every CI
# run, so that a typo here cannot silently open a hole.
#
# THIS IS A RATCHET. These numbers come down as the code improves. They are never raised to
# accommodate new code: a violation is a conversation about the code, not about the number.
#
# Format: 'RuleId'(optional 'SymbolKind'): 'Threshold'. The rule fires when the measured value
# exceeds the threshold (CA1505 fires when the maintainability index falls BELOW it).
#
# WARNING — the only valid SymbolKind spellings are the ones below. An unrecognised entry (e.g.
# `NamedType` instead of `Type`) makes the analyzers stop reporting ENTIRELY AND SILENTLY: no
# CA1509 diagnostic is produced. That failure mode is why scripts/CodeMetrics.cs refuses
# to run at all when this file does not parse.

# Cyclomatic complexity per method. 25 is the .NET default, pinned here so an SDK update cannot
# move the gate. Mark Seemann's recommended maximum is 7 (Miller's 7 +/- 2); this is 3.6x that.
# No method exceeds it: the two grandfathered TargetParser methods were decomposed by #263, and
# the repository worst sits exactly at the threshold (ArrowMappingComponent.TryMap, CC 25). The
# next ratchet step waits for a refactor of that method, not a number tuned to pass.
CA1502: 25

# Maintainability index. Fires below 10. The worst member in the repository today is 27
# (SortedRowGroupPruningComponent.EmitPruningHelpers) — #263 moved the floor from 14 to 27, so
# this gate has seventeen points of headroom.
CA1505: 10

# Class coupling. .NET defaults, pinned. Worst today: 66 for a type (TargetParser) and 32 for a
# method (TargetParser.BuildCompoundModel). The method gate had zero headroom before #263
# (GetTargetModelCore sat at exactly 40); the decomposition gave it eight points back.
CA1506(Type): 95
CA1506(Method): 40
